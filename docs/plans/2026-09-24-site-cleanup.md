# Site cleanup, two lanes (2026-09-24)

Dixon, 2026-09-24: "lets get website fully cleaned up merged and deployed all things ready." Source: the NEEDS WORK list in `docs/audits/2026-09-24-site-review.md` (analytics + live QA + visual read) and the 09-22 audit's NOT DONE list. The drop-in page (`/schedule/`) is already merged and live (d12c8ca): do not touch `#page-schedule`, `js/schedule.js` or the schedule CSS in either lane.

Two builders work in parallel, each in its own worktree and branch, on disjoint sections. Martha merges both, regenerates shells and CSS, sweeps, and deploys. **Neither lane pushes or commits to `main`.**

## Rules for both lanes
- Read first: `.claude/CLAUDE.md` (repo), `C:\Users\bowle\.claude\design-taste.md`, this file, and the review record above.
- Edit `index.html` only inside your lane's sections, then `node scripts/build-routes.mjs` + `--check`. Edit `css/site.css`, then `node scripts/build-css.mjs` + `--check`. Never hand-edit a generated shell. (Both lanes will regenerate the shells and `site.min.css`; Martha resolves those at merge by rebuilding, so commit them normally.) Put new CSS in a clearly commented block for your lane at the END of `css/site.css` (`/* ═══ 2026-09-24 cleanup, lane A: ProSeries ═══ */` or `lane B`) rather than editing rules scattered through the file, unless you are deleting a rule. That keeps the two lanes' CSS diffs from colliding.
- Keep every piece of Dixon's written copy as is (body paragraphs, FAQ answers, bios, pricing text). You may change headings' casing/treatment, labels, and short UI strings named below. No new prose beyond the short strings named here. Voice: plain, warm, direct, no em dashes, no emoji. Say "apply", never "sign up", for ProSeries enrollment.
- Brand: forest `#0c1f17` ground; Family Pink `#FF7AA2` is the one primary (one filled button per view); terracotta `#C8614B` display only, small text on forest uses `#d4775f`; seafoam `#6BAF8A` + soft pink `#FF8FAB` for dwdPS track badges; Cormorant Garamond display, Outfit body, Bebas Neue for large numerals only. Gold only in the Tamara Mark (and the sanctioned v3 logo sparkle). **No cream by default** (Dixon 2026-09-23): ivory `#FAF3E8` is at most one deliberate feature surface per page, never a page ground. Hierarchy by size, weight and space; max 2 sizes + 2 weights per component; spacing from 4/8/12/16/24/32/48/64; no boxes inside boxes; never center-align body text.
- QA with Puppeteer (`require('C:/Users/bowle/Code/DWDC-Instagram-Posts/node_modules/puppeteer')`, `channel: 'chrome'`) against a static server your own script starts and stops (lane A port 8792, lane B port 8793). Block `/sw.js`. Intercept every non-read Supabase call and anything to `drop-in-checkout` with a fake response that includes `Access-Control-Allow-Origin: *` (and fake the OPTIONS preflight to `drop-in-checkout` too). Let `rpc/public_*` reads and GET reads pass. Nothing reaches the live database, Stripe or email.
- Git Bash turns `>` inside some quoted commands into empty junk files in the cwd: check `git status` before every commit and delete empty stray files you created. Commit with explicit paths, never `git add -A`. Commit messages end with `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>`. Push your branch when done.
- Iterate on the look at least three times against design-taste.md before reporting (squint test, deletion pass). "Checks pass" is not "looks good".

## Lane A: ProSeries on one ground
Worktree `C:\Users\bowle\Code\dwd-website-ps`, branch `cleanup-proseries`. Owns `#page-proseries` in `index.html`, the ProSeries rules in `css/site.css`, and `js/episodes.js` / `js/season.js` only if a class name they render changes.

Today the page (19,200px desktop, 15,700px phone) changes ground about ten times: navy Season One hero, forest drop-in panel, forest tracks, ivory "One-time fees", hot-pink "How deposits work" + "What a full season adds up to", forest chairs, a hot-pink "Now casting" band holding the interest form, forest cast and story, ivory "The Program", a "Why ProSeries" stack of four differently coloured boxes (terracotta, ivory, pink, forest), forest FAQ and philosophy, a hot-pink Episode Guide. Headings come in four treatments (sky-blue Cinzel caps, pink italic serif caps, Cormorant caps, Outfit caps).

The agreed direction (STATUS "Agreed", 09-22 audit): **one forest ground for the whole page; the sky-blue Season One title treatment lives inside the hero only; pink is the accent, not a band colour; ivory only for the money.**
1. Ground: forest end to end. Kill the hot-pink bands (deposits, now casting, episode guide) and the ivory Program section; their content sits on forest. Section separation comes from space (64px+ between sections at 390) and at most a hairline, not colour blocks.
2. Money: the one deliberate ivory surface on the page is a single contiguous money block (one-time fees + how deposits work + what a full season adds up to). If after your three passes a raised forest panel reads better than ivory there, use that and say so in one line; do not leave two ivory surfaces.
3. Headings: one section-heading treatment for the whole page (Cormorant Garamond, one size, one weight; caps or sentence case, pick one and apply it everywhere) plus the existing small kicker label style. The Season One hero keeps its sky-blue title card. Bebas stays for prices and big numerals.
4. "Why ProSeries": the four coloured boxes become one list on forest (heading + paragraph per item, separated by space or a hairline). No boxes inside boxes.
5. Interest form ("Now casting" / "Tell me about your dancer"): on forest, fields must be clearly visible (a filled field with a 1px border, labels above, like the checkout form on /schedule/), submit is the Family Pink primary, "+ Add another dancer" a secondary outline. Every label and help text passes contrast on forest (4.5:1 for small text).
6. Episode Guide: episode cards on forest, the current/next episode marked with the existing UP NEXT treatment; the pink band goes.
7. Keep section order, the chapter rail, the drop-in panel near the top, and all copy. Keep the cast name grid and chair chips (restyle only if they collide with the new ground).
8. Check the ProSeries phone length does not grow by more than ~10% (target in STATUS: prices one screen down, form about five screens down).

Verification screenshots to `C:\Users\bowle\AppData\Local\Temp\claude\C--Users-bowle\ce052655-b9a8-415d-bd4d-292a4090a7e5\scratchpad\laneA\`: before and after, full page at 390 (tile it into 1300px-tall PNGs named `before-390-NN.png` / `after-390-NN.png`) and the first two screens at 1280 (`before-1280.png`, `after-1280.png`), plus `form-390.png` (interest form, empty and with validation errors showing). `checks.json`: `{ "build_routes_check", "build_css_check", "console_errors": [], "failed_requests": [], "ground_changes_390": n, "heading_treatments": n, "ivory_surfaces": n, "phone_height_before", "phone_height_after", "no_live_writes": bool }`.

## Lane B: everything else
Worktree `C:\Users\bowle\Code\dwd-website-clean`, branch `cleanup-site`. Owns `#page-home`, `#page-adult-company` (the Collective), `#page-teachers`, `#page-contact`, the footer, the `<head>` of `index.html`, and `js/main.js`, `js/now.js`, `js/dwdc-next.js`, plus their CSS.

1. **Home.**
   - Delete the "THIS WEEK" block (`#now`, the ProSeries "Season One is underway / 13 chairs open" row and the Collective "Adult modern company" row). The "Find your path" cards right below say the same things, and open chairs currently appear three ways on one page. The DROP IN panel stays. Make sure `js/now.js` (which also renders the drop-in panel, the sticky bar label and the ProSeries chair chips) still works with `#now` gone: every lookup must tolerate the missing block.
   - Dead space: at 390 there are three empty forest bands of roughly 250px (around the Season One band, before and after the Tokyo Sunrise video, around the quote). Bring section spacing onto the scale (48 to 64px between sections on a phone).
   - "Original choreography by Dixon Bowles · 2024 season." is right-aligned italic: align it with its heading.
   - The quote's highlighted word ("company") has a terracotta box behind it: remove the box (decoration without a job); the italic colour can stay.
2. **Collective** (`/collective/`). There is no next class date yet (Dixon has a reminder). Three adults have asked to join through the contact form this summer. Make it one joining screen, then history:
   - Top of the page (after the hero): what it is, who it's for (18+, all levels, no audition), where (Exchange Dance, 7409 Chancery Lane, Orlando), cost ($15 a class, pay at class: cash or Venmo), and the next class. With no date in `public_site_dwdc_events`, the next-class line reads `Next class: date coming soon.` with one primary action `Join the Collective` linking to `/contact/?reason=adult` (that pre-selects Adult Company on the contact form; the three real leads came in that way). When `js/dwdc-next.js` does find a future event it must render that date prominently in the same spot, so the page updates itself once Dixon schedules a class in the Director. Read `js/dwdc-next.js` and keep its contract.
   - Everything about past shows (A·Muse in Space, June 2026; Before the Collective / Beastly Kingdom, 2025, the video, the photo grid, "What it took") goes behind one collapsed toggle, `Past shows`, below the joining screen. Keep all of it, just collapsed.
   - Body copy left-aligned everywhere on the page (it is centred today). The Collective keeps its hushed ghost-button style for secondary actions (agreed in STATUS); the one `Join the Collective` action is the page's filled primary.
   - Target: the phone page's first two screens answer what/who/where/cost/next/join; total phone height roughly halves (9,000px today) with history collapsed.
3. **Teachers.** Tori Ugalde and John Nuzzi show as big empty tiles with initials ("TU", "JN") until professional photos arrive (Dixon has a reminder). Replace the tiles with a compact text entry (name, role, one line) under "Company assistants", no image box. Leave an HTML comment where the photo goes so dropping in `images/...` later is a two-line change.
4. **Contact.**
   - Fields are underline-only and nearly invisible on forest: give them the same filled field + 1px border + label-above treatment as the checkout form on /schedule/ (read `#page-schedule` form CSS and match it; do not edit it).
   - Add proper space (24px) between the "What's this about?" pills and the MESSAGE label.
   - The two Instagram handles in the Connect list render at different sizes: same size.
5. **Footer.** On phones the `|` separator before "Privacy policy" wraps onto its own line: hide it under the wrap width (or restructure so it never orphans).
6. **Speed.** Video posters and loops that live in sections other than the active route download on every page (143KB on Home alone, 29% of the page; they delay first paint on slow 4G). Only the active route's media may load: move off-route `poster`/`src` to `data-` attributes and set them when `showPage` makes that section active (and on initial load for the shell's own section). Verify with a request log on a cold load of `/`, `/schedule/`, `/collective/` at 390 that no other route's poster or video is requested.
7. **Legacy hash links.** `/#proseries` and `/#schedule` (old links) paint Home for 0.5 to 0.9s before switching. Add a tiny inline script at the top of `<head>` that sends a known page hash straight to its real path with `location.replace` before first paint (reuse the page list and the legacy map in `js/main.js`; keep in-page anchors like `#ps-faq` working). `/#contact?reason=adult` must still land on the contact form with Adult Company selected.

Verification screenshots to `C:\Users\bowle\AppData\Local\Temp\claude\C--Users-bowle\ce052655-b9a8-415d-bd4d-292a4090a7e5\scratchpad\laneB\`: before/after at 390 for Home (full page, tiled 1300px), Collective (full page tiled, and with Past shows opened), Teachers (the assistants area), Contact (the form), footer; first screen at 1280 for Home, Collective, Contact. `checks.json`: `{ "build_routes_check", "build_css_check", "console_errors": [], "failed_requests": [], "home_height_390_before/after", "collective_height_390_before/after", "offroute_media_requests": { "/": [...], "/schedule/": [...], "/collective/": [...] }, "legacy_hash_flash_ms": n, "contact_reason_adult_ok": bool, "dwdc_next_with_event_ok": bool (stub one future event in the intercepted response to prove the date renders), "no_live_writes": bool }`.

## Report format (both lanes)
Five lines max on what changed, branch + commit, screenshot and checks.json paths, then one line per decision this brief did not cover.
